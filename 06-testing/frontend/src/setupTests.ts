// Questo file viene caricato da Vitest prima dei test.
// jest-dom aggiunge matcher leggibili per il DOM, per esempio:
// expect(element).toBeInTheDocument()
import "@testing-library/jest-dom/vitest";

